# dependencies
node_modules

# next.js
.next
out
next-env.d.ts

# build output
dist
build
coverage

# Playwright (e2e/) output
test-results/
playwright-report/
blob-report/

# generated Prisma client (regenerated via `npm run db:generate`)
lib/generated/

# misc
*.log
.env
.env*.local
.DS_Store
*.tsbuildinfo

# Claude Code / AI tooling — not part of the shipped app
.agents/
.claude/
.design-sync/
.ds-sync/
ds-bundle/
skills-lock.json

# Graphify — salida y cache del analisis de codigo (se regenera con `graphify`)
graphify-out/
